# Scry Me a Query (Discord Bot)
This bot provides additional Scryfall functionality to discord like returning multiple items from a query.

## Commands
- `!squery {query}` - Takes a scryfall query and returns images of the cards that match the result along with a link to the query in scryfall

## Permissions
TO DO: Fill it out